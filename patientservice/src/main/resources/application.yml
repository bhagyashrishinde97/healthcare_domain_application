server:
  port: 8082

spring:
  application:
    name: patientservice

  datasource:
    url: jdbc:postgresql://localhost:5432/healthinsurance
    username: postgres
    password: root123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/healthcare-realm



#client secret : AAHrLPSJXVNPFbM8MaV5njWluEQoVBaw